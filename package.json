{
    "name": "convert-to-draft-on-change-request",
    "version": "1.0.0",
    "description": "GitHub action to convert a open pull request to a draft pull request on change request",
    "main": "dist/index.js",
    "scripts": {
        "build": "ncc build src/index.js -o dist",
        "tests": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@actions/core": "^1.10.1",
        "@actions/github": "^6.0.1"
    },
    "keywords": [
        "github",
        "action",
        "pull-request",
        "draft"
    ],
    "author": "Jack Allen",
    "license": "GPL-3.0-only",
    "devDependencies": {
        "@vercel/ncc": "^0.38.3"
    }
}
